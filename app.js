let firstName = prompt("ismingizni kiriting");
let surName = prompt("familyangizni kiriting");
const educationCenter = "Najot ta'lim";
alert(
  `Salom ${firstName.charAt().toUpperCase()}${firstName
    .slice(1)
    .toLowerCase()}  ${surName.charAt().toUpperCase()}${surName
    .slice(1)
    .toLowerCase()} ${educationCenter}ga hush kelibsz`
);
